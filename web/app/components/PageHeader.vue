<!--
  Шапка страницы: заголовок + короткое пояснение под ним.
  Раньше этот блок был скопирован на каждой из семи страниц одинаково -
  вынесено сюда, чтобы менять стиль шапки в одном месте, а не в семи.

  Заголовок - через слот по умолчанию, а не пропс: на странице конкретного
  расчёта в заголовке сидит подсвеченный ID (<span class="num">...</span>),
  и слот пропускает такую разметку насквозь, а обычный текстовый пропс - нет.

  Пояснение (.page__lead) необязательно и рендерится, только если передан
  слот "lead" - у страницы истории конкретного расчёта его нет вообще.
-->
<template>
  <div class="page__head">
    <h1><slot /></h1>
    <p v-if="$slots.lead" class="page__lead"><slot name="lead" /></p>
  </div>
</template>
