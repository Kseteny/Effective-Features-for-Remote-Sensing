<script setup lang="ts">
useHead({ title: 'Методы отбора — Отбор признаков' })
</script>

<template>
  <article class="doc">
    <h1>Методы отбора</h1>

    <p>
      Оба критерия работают по схеме прямого последовательного отбора:
      признаки добавляются по одному, каждый раз выбирается тот, что даёт
      наибольший прирост. Отбор останавливается, когда прирост становится
      незначительным.
    </p>

    <h2>Расстояние Бхаттачарьи</h2>
    <p>
      Фильтровый метод: оценивает признаки сами по себе, без обучения
      классификатора. Считает, насколько статистически разделимы два класса
      в пространстве выбранных признаков.
    </p>
    <p>
      Важное ограничение — метод попарный. Он оценивает разделимость
      конкретной пары классов, а не всех сразу: расстояние Бхаттачарьи
      математически определено для двух распределений. По умолчанию берётся
      пара «разреженная застройка» и «леса» как показательный случай.
    </p>

    <h2>Отбор через kNN</h2>
    <p>
      Метод-обёртка: на каждом шаге обучает классификатор ближайших соседей
      и смотрит на его точность по кросс-валидации. В отличие от Бхаттачарьи
      учитывает все классы одновременно, поскольку точность — естественная
      многоклассовая метрика.
    </p>
    <p>
      Расплата за это — время. Обучение классификатора на каждом шаге для
      каждого признака-кандидата обходится на порядки дороже, чем расчёт
      расстояния по формуле.
    </p>

    <h2>Как оценивается результат</h2>
    <p>
      Итоговое качество обоих наборов измеряется одинаково: отобранные
      признаки подаются классификатору, обучение идёт на 70% данных,
      проверка — на отложенных 30%. Так наборы, полученные разными
      критериями, можно сравнивать напрямую.
    </p>
  </article>
</template>

<style scoped>
.doc { padding: 2rem; max-width: 720px; line-height: 1.65; }
h1 { margin-top: 0; }
h2 { margin-top: 2rem; font-size: 1.15rem; }
</style>